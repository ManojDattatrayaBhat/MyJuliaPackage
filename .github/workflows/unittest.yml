name: Unittest
on:
  push:
    branches:
      - main
    tags: ['*']
  pull_request:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    needs: CI
    steps:
      - uses: julia-actions/julia-runtest@v1
